steps:
- task: Docker@2
  displayName: 'Build and Push'
  inputs:
    containerRegistry: $(registry)
    repository: $(repository)
    Dockerfile: $(dockerfile)
    tags: |
     $(Build.BuildId)
     latest